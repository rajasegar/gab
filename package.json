{
  "name": "github-actions-builder",
  "version": "1.2.0",
  "description": "A Cli tool to build github actions workflow from wherever you are",
  "main": "index.js",
  "bin": {
    "gab": "bin/cli.js",
    "github-actions-builder": "bin/cli.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "deploy": "npm version patch && git push && git push --tags && npm publish"
  },
  "keywords": [
    "github-actions",
    "cli",
    "workflow"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@octokit/rest": "^18.0.0",
    "dotenv": "^8.2.0",
    "inquirer": "^7.1.0"
  },
  "devDependencies": {
    "eslint": "^7.1.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-prettier": "^3.1.3",
    "prettier": "^2.0.5"
  }
}
